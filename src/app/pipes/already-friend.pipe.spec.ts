import { AlreadyFriendPipe } from './already-friend.pipe';

describe('AlreadyFriendPipe', () => {
  it('create an instance', () => {
    const pipe = new AlreadyFriendPipe();
    expect(pipe).toBeTruthy();
  });
});
